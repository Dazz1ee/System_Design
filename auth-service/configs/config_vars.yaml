worker-threads: 4
worker-fs-threads: 2
logger-level: debug

is-testing: ture

server-port: 8081

public-key-path: ../app/keys/public.pem
private-key-path: ../app/keys/private.pem

# change to URI of your postgres database
pg-connection: postgresql://testsuite@localhost:15433/auth_service_db_1