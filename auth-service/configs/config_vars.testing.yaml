worker-threads: 2
worker-fs-threads: 2
logger-level: debug

is-testing: true

server-port: 8081

public-key-path: ../keys/public.pem
private-key-path: ../keys/private.pem

# change to URI of your postgres database
pg-connection: postgresql://testsuite@localhost:15433/auth